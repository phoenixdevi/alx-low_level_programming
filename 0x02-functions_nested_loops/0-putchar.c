#include <stdio.h>
#include "main.h"

int main(void)

{
	int ch;

      ch = '_';
      putchar(ch);
      ch = 'p';
      putchar(ch);
      ch = 'u';
      putchar(ch);
      ch = 't';
      putchar(ch);
      ch = 'c';
      putchar(ch);
      ch = 'h';
      putchar(ch);
      ch = 'a';
      putchar(ch);
      ch = 'r';
      putchar(ch);
      ch = '\n';
      putchar(ch);

      return(0)
}

